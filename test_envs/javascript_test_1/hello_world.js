// Define a function to return a greeting message
function getGreeting(name) {
    return `Hello, ${name}!`;
}

// Define a main function to print the greeting message to the console
function main() {
    const name = 'World';
    const message = getGreeting(name);
    console.log(message);
}

// Call the main function to execute the program
main();
