This test env is a base for test "whether clangd as LSP server works". I want to make sure the LSPs we are using are not
limited to rust-analyzer, and from clangd seemed like an easy thing to test with.

In order to run this test, clang and clangd packages are necessary (debian).