#[derive(Debug, Clone, Copy)]
pub enum Arrow {
    Up,
    Down,
    Left,
    Right,
}

#[derive(Debug, Clone, Copy)]
pub enum VerticalArrow {
    Up,
    Down,
}

#[derive(Debug, Clone, Copy)]
pub enum HorizontalArrow {
    Left,
    Right,
}
